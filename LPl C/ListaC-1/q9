#include <stdio.h> 

int main(){
    float lado, area;
    
    printf("Digite o lado do quadrado: ");
    scanf("%f", &lado);
    
    area = lado * 2;
    printf("A área do quadrado é: %f", area);
    
    return 0;
}